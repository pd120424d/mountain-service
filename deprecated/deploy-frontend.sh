#!/bin/bash
# Deprecated: legacy docker-compose frontend deployment retained for reference.

cat <<'LEGACY'
Original script content preserved in repo history. Refer to legacy deployment process if needed.
LEGACY

